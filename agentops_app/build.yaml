# Build configuration for code generation
# Note: bdd_widget_test does NOT provide a builder for auto-generating tests from .feature files
# .feature files serve as specification/documentation only
# Dart test files must be written manually following BDD patterns

targets:
  $default:
    builders:
      # Builders configured by other packages
      riverpod_generator:
      freezed:
      json_serializable:
